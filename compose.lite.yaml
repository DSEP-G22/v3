# Laptop profile: docker compose -f compose.yaml -f compose.lite.yaml up -d --wait
# Every stage runs, translation and speech included: the whole stack needs about 4 GB of RAM.
# Kept so the documented command keeps working; it no longer switches anything off. (Capping
# speech to two CPUs made a 20 second voice note take 20 seconds to transcribe instead of 4.)
services: {}
