; Copy to inventory.ini and fill in. One host is enough: the stack is one compose project.
[lanka]
lanka-prod ansible_host=203.0.113.10 ansible_user=ubuntu

[lanka:vars]
site_domain=lankalink.example.lk
acme_email=ops@example.lk
github_owner=dsep-g22
; Everything secret is passed with -e at run time (the deploy workflow does this), never written here.
