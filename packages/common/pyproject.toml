[project]
name = "lanka-common"
version = "3.0.0"
requires-python = ">=3.12"
dependencies = ["asyncpg>=0.29", "nats-py>=2.9", "langsmith>=0.2"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["lanka_common"]
