[project]
name = "lanka-knowledge"
version = "3.0.0"
requires-python = ">=3.12"
dependencies = ["lanka-common", "fastapi>=0.115", "uvicorn[standard]>=0.30", "httpx>=0.27", "numpy>=1.26",
                "pyyaml>=6", "pydantic>=2.6", "nats-py>=2.9"]

[project.optional-dependencies]
models = ["fastembed>=0.4"]

[tool.uv]
package = false

[tool.uv.sources]
lanka-common = { workspace = true }
