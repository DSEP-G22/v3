[project]
name = "lanka-inquiry"
version = "3.0.0"
requires-python = ">=3.12"
dependencies = [
  "lanka-common",
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "python-multipart>=0.0.9",
  "asyncpg>=0.29",
  "nats-py>=2.9",
  "redis>=5",
  "minio>=7.2",
]

[tool.uv]
package = false

[tool.uv.sources]
lanka-common = { workspace = true }
