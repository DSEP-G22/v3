[project]
name = "lanka-business"
version = "3.0.0"
requires-python = ">=3.12"
dependencies = [
  "lanka-common",
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "sqlalchemy[asyncio]>=2.0",
  "asyncpg>=0.29",
  "pyyaml>=6",
  "nats-py>=2.9",
  "redis>=5",
  "httpx>=0.27",
]

[tool.uv]
package = false

[tool.uv.sources]
lanka-common = { workspace = true }
