[project]
name = "lanka-orchestrator"
version = "3.0.0"
requires-python = ">=3.12"
dependencies = ["lanka-common", "fastapi>=0.115", "uvicorn[standard]>=0.30", "asyncpg>=0.29", "nats-py>=2.9", "httpx>=0.27"]

[tool.uv]
package = false

[tool.uv.sources]
lanka-common = { workspace = true }
