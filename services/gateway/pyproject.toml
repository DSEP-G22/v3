[project]
name = "lanka-gateway"
version = "3.0.0"
requires-python = ">=3.12"
dependencies = [
  "lanka-common",
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "pyjwt[crypto]>=2.9",
  "httpx>=0.27",
  "nats-py>=2.9",
  "redis>=5",
]

[tool.uv]
package = false

[tool.uv.sources]
lanka-common = { workspace = true }
