[project]
name = "lanka-audio"
version = "3.0.0"
requires-python = ">=3.12"
dependencies = ["lanka-common", "fastapi>=0.115", "uvicorn[standard]>=0.30", "httpx>=0.27", "numpy>=1.26"]

[project.optional-dependencies]
models = ["faster-whisper>=1.0"]

[tool.uv]
package = false

[tool.uv.sources]
lanka-common = { workspace = true }
